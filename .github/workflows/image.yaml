name: image-test

on: workflow_dispatch

jobs:
  fedora:
    runs-on: ["runner-scale-set"]

    container:
      image: fedora:latest

    steps:
      - name: /etc/os-release
        run: cat /etc/os-release

  ubuntu:
    runs-on: ["runner-scale-set"]

    # run outside container if possible
    # container:
    #   image: ubuntu:latest

    steps:
      - name: /etc/os-release
        run: cat /etc/os-release
